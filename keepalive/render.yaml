services:
  - type: web
    name: garvix-keepalive
    runtime: node
    rootDir: keepalive
    buildCommand: npm install
    startCommand: npm start
    plan: free
    envVars:
      - key: PORT
        value: 3000
